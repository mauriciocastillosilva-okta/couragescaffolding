define([
  '<%= proj() %>/models/<%= ctor() %>'
], function (<%= ctor() %>) {

    describe('<%= proj() %>/models/<%= ctor() %>', function () {

      it('describe some behavior', function () {
        expect('this').toBe('that');
      });
      
    });
});